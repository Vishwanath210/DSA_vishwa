#include<stdio.h>
int main()
{
    int a,b;
    printf("enter two number");
    scanf("%d%d",&a,&b);
    if(a>b)
        printf("%d is a maximum\n",a);
    else
        printf("%d is a maximum\n",b);
        printf("program for maximum of two number");
    return 0;
}
